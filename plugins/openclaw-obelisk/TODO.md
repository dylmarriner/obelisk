# OpenClaw plugin TODO

- Run `npm run plugin:validate` in a real OpenClaw workspace.
- Confirm the exact `api.registerTool` optional-tool runtime signature for `definePluginEntry`.
- If OpenClaw validation prefers pure metadata generation, split the approval hook into a follow-up mixed plugin or migrate to `defineToolPlugin`.
- Add integration tests once OpenClaw plugin SDK test helpers are available in CI.
