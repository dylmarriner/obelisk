# Extra OpenClaw notes

The OpenClaw plugin package is intentionally tool-first. Do not treat Obelisk as a provider, channel, or CLI backend unless OpenClaw adds a dedicated context-engine/plugin capability later.
